# Optional: enables live GPT-5.6 Sol diagnosis and fresh-session agent turns.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol

# Set this to the deployed origin so social metadata resolves to the live site.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
